title = "Wade's Blog"
baseURL = "https://pwhau.github.io/"

# This is what goes in <html lang="">
languageCode = "en-us"

# This defines how dates are formatted
defaultContentLanguage = "zh-tw"

# Enable emojis globally
enableEmoji = true
ignoreErrors = ["additional-script-loading-error"] # ignore error of loading additional scripts.

# traditional way: theme component resides in directory 'themes'
theme = "hugo-blog-awesome"

# modern way: pull in theme component as hugo module
#[module]
  # Uncomment the next line to build and serve using local theme clone declared in the named Hugo workspace:
  # workspace = "hugo-blog-awesome.work"
  #[module.hugoVersion]
    #extended = true
    #min = "0.87.0"
  #[[module.imports]]
    #path = "github.com/hugo-sid/hugo-blog-awesome"
    #disable = false


#[services]
# To enable Google Analytics 4 (gtag.js) provide G-MEASUREMENT_ID below.
# To disable Google Analytics, simply leave the field empty or remove the next two lines
#  [services.googleAnalytics]
#    id = '' # G-MEASUREMENT_ID

# To enable Disqus comments, provide Disqus Shortname below.
# To disable Disqus comments, simply leave the field empty or remove the next two lines
#  [services.disqus]
#    shortname = 'pwhau'

# set markup.highlight.noClasses=false to enable code highlight
[markup]
  [markup.highlight]
    noClasses = false
  [markup.goldmark.renderer]
    unsafe = true
	[markup.tableOfContents]
		startLevel = 2 # ToC starts from H2
		endLevel = 4 # ToC ends at H4
		ordered = false # generates <ul> instead of <ol>

############################## English language ################################
[Languages.en-gb]
  languageName = "English"
  languageCode = "en-gb"
  contentDir = "content/en"
  weight = 1

  [Languages.en-gb.menu]
  [[Languages.en-gb.menu.main]]
    # The page reference (pageRef) is useful for menu highlighting
    # When pageRef is set, setting `url` is optional; it will be used as a fallback if the page is not found.
    pageRef="/"
    name = 'Home'
    url = '/'
    weight = 10

  [[Languages.en-gb.menu.main]]
    pageRef="/custom/guidemap.html"
    name = 'Map'
    url = '/custom/guidemap.html'
    weight = 20

  [[Languages.en-gb.menu.main]]
    pageRef="posts"
    name = 'Posts'
    url = '/posts/'
    weight = 30

  [[Languages.en-gb.menu.main]]
    pageRef="about"
    name = 'About'
    url = '/about/'
    weight = 40

  [[Languages.en-gb.menu.main]] # --- custom page en --- #
    pageRef= "pages" # age(content)Ref
    name = 'Pages' # navigation name 
    url = '/pages/' # dir_address optional
    weight = 50

  [[Languages.en-gb.menu.main]] # --- custom page en --- #
    pageRef= "now" # age(content)Ref
    name = 'Now' # navigation name 
    url = '/now/' # dir_address optional
    weight = 60

    

[Languages.en-gb.params]
  sitename = "WADE'S BLOG"
  defaultColor = "dark" # set color mode: dark, light, auto
  # Setting it to 'auto' applies the color scheme based on the visitor's device color preference.If you don't specify anything, ignore this parameter, or leave it blank,
  # the default value is set to 'auto'.
  # You can take a look at layouts/index.html for more information.
  description = "Minimal Hugo blog theme with light and dark mode support"
  mainSections = ['posts']
  toc = false # set to false to disable table of contents 'globally'
  tocOpen = false # set to true to open table of contents by default
  goToTop = true # set to false to disable 'go to top' button
  additionalScripts = ['js/custom.js', 'js/custom-2.js']
  # Will try to load 'assets/js/custom.js' and 'assets/js/custom-2.js'.
  # Your custom scripts will be concatenated to one file `custom.js`.
  # When building for production it will be minified.
  # The file `custom.js` is loaded on each page (before body tag ends).
  dateFormat = "" # date format used to show dates on various pages. If nothing is specified, then "2 Jan 2006" format is used.
  # See https://gohugo.io/functions/time/format/ for available date formats.
  rssFeedDescription = "summary" # available options: 1) summary 2) full
  # summary - includes a short summary of the blog post in the RSS feed. Generated using Hugo .Summary .
  # full - includes full blog post in the RSS feed. Generated using Hugo .Content .
  # By default (or if nothing is specified), summary is used.

[Languages.en-gb.params.author]
  avatar = "avatar.jpg" # put the file in assets folder; also ensure that image has same height and width
  # Note: image is not rendered if the resource(avatar image) is not found. No error is displayed.
  intro = "WADE'S BLOG"
  name = "Wade"
  description = "Travel stories and overseas life diaries. Welcome to start with the guide map."

# Allow to override webmanifest options
[Languages.en-gb.params.webmanifest]
  name = "sitename" # will use "params.sitename" or "title" by default
  short_name = "sitename" # same as name
  start_url = "/" # will use homepage url by default
  theme_color = "#434648" # default is "#434648" (base color of text). Also will override html `<meta name="theme-color" />`
  background_color = "#fff" # by default depend on "params.defaultColor" for "light" or "auto" will be set to "#fff" for dark will be "#131418" (color of dark mode background)
  display = "standalone"

############################## Mandarin language ################################
[Languages.zh-tw]
  languageName = "Mandarin"
  languageCode = "zh-tw"
  contentDir = "content/zh-tw"
  weight = 1

  [Languages.zh-tw.menu]
  [[Languages.zh-tw.menu.main]]
    # The page reference (pageRef) is useful for menu highlighting
    # When pageRef is set, setting `url` is optional; it will be used as a fallback if the page is not found.
    pageRef="/"
    name = '首頁'
    url = '/'
    weight = 10
#  [[Languages.zh-tw.menu.main]]
#    pageRef="/static/custom/guidemap.html"
#    name = '地圖'
#    url = 'custom/guidemap.html'
#    weight = 20
  [[Languages.zh-tw.menu.main]]
    pageRef="posts"
    name = '文章'
    url = '/posts/'
    weight = 30
  [[Languages.zh-tw.menu.main]]
    pageRef="about"
    name = '關於'
    url = '/about/'
    weight = 40
  [[Languages.zh-tw.menu.main]] # --- custom page en --- #
    pageRef= "now" # age(content)Ref
    name = '近況更新' # navigation name 
    url = '/now/' # dir_address
    weight = 50
  [[Languages.zh-tw.menu.main]] # --- custom page en --- #
    pageRef= "pages" # age(content)Ref
    name = '目錄列' # navigation name 
    url = '/pages/' # dir_address
    weight = 60


[Languages.zh-tw.params]
  sitename = "WADE'S BLOG"
  defaultColor = "dark" # set color mode: dark, light, auto
  # Setting it to 'auto' applies the color scheme based on the visitor's device color preference.If you don't specify anything, ignore this parameter, or leave it blank,
  # the default value is set to 'auto'.
  # You can take a look at layouts/index.html for more information.
  description = "支援亮色和暗色模式，語言切換功能(若內容支援)"
  mainSections = ['posts']
  toc = false # set to false to disable table of contents 'globally'
  tocOpen = false # set to true to open table of contents by default
  goToTop = true # set to false to disable 'go to top' button
  additionalScripts = ['js/custom.js', 'js/custom-2.js']
  # Will try to load 'assets/js/custom.js' and 'assets/js/custom-2.js'.
  # Your custom scripts will be concatenated to one file `custom.js`.
  # When building for production it will be minified.
  # The file `custom.js` is loaded on each page (before body tag ends).
  dateFormat = "2006年01月02日" # date format used to show dates on various pages. If nothing is specified, then "2 Jan 2006" format is used.
  # See https://gohugo.io/functions/format/#hugo-date-and-time-templating-reference for available date formats.
  rssFeedDescription = "summary" # available options: 1) summary 2) full
  # summary - includes a short summary of the blog post in the RSS feed. Generated using Hugo .Summary .
  # full - includes full blog post in the RSS feed. Generated using Hugo .Content .
  # By default (or if nothing is specified), summary is used.

[Languages.zh-tw.params.author]
  avatar = "avatar.jpg" # put the file in assets folder; also ensure that image has same height and width
  # Note: image is not rendered if the resource(avatar image) is not found. No error is displayed.
  intro = "WADE'S BLOG"
  name = "Wade"
  description = "旅遊故事以及海外生活日記。歡迎從導覽地圖開始。---從0開始錄Podcast-聊聊Wade什麼？---"

# Allow to override webmanifest options
[Languages.zh-tw.params.webmanifest]
  name = "sitename" # will use "params.sitename" or "title" by default
  short_name = "sitename" # same as name
  start_url = "/" # will use homepage url by default
  theme_color = "#434648" # default is "#434648" (base color of text). Also will override html `<meta name="theme-color" />`
  background_color = "#fff" # by default depend on "params.defaultColor" for "light" or "auto" will be set to "#fff" for dark will be "#131418" (color of dark mode background)
  display = "standalone"

###################################################################

# Allow to override `browserconfig.xml` params (configuration for windows embedded browsers)
[params.browserconfig]
  TileColor = "#2d89ef" # default windows 10 blue tile color



[[params.socialIcons]]
name = "googleform"
url = "https://forms.gle/hLTEpXKkypfvcNJY9"

[[params.socialIcons]]
name = "email"
url = "https://poweiwade.substack.com/?r=6k5qqo&utm_campaign=pub-share-checklist"

[[params.socialIcons]]
name = "instagram"
url = "https://www.instagram.com/tellwadewhy/"

[[params.socialIcons]]
name = "applemusic"
url = "https://podcasts.apple.com/us/podcast/%E8%81%8A%E8%81%8Awade%E4%BB%80%E9%BA%BC/id1837625610"

[[params.socialIcons]]
name = "youtube"
url = "https://www.youtube.com/@Tellwadewhy/podcasts"

[[params.socialIcons]]
name = "spotify"
url = "https://creators.spotify.com/pod/profile/tellwadewhy/"

[[params.socialIcons]]
name = "codepen"
url = "https://codepen.io/powei_wade"

[[params.socialIcons]]
name = "other"
url = "https://linktr.ee/powei_wade"

# [[params.socialIcons]]
# name = "Rss"
# url = "/index.xml"
